<p id="notice"><%= notice %></p>
<h2><%= @post.title %></h2>
<p><%= @post.body %></p>

<% if current_user.id == @post.user_id %><%= link_to 'Edit', edit_post_path(@post) %> |<% end %> <%= link_to 'Back', posts_path %>


<h2>Comments</h2>

<%= render "comments/comments" %>
<%= render "comments/form" %>